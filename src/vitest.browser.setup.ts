import { beforeAll } from 'vitest'

// Browser environment setup
// This file runs before browser-based component tests

beforeAll(() => {
	// Browser environment is already set up by Vitest
	// Add any browser-specific global setup here if needed
	// e.g., polyfills, global mocks, custom window properties, etc.
})
